import 'package:flutter/material.dart';

class MarginForBtn extends StatelessWidget {
  const MarginForBtn({super.key});
  // ボタンの構成
  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}